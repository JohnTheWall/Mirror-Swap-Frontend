# Frontend for the MirrorSwap smart contract protocol.

## Code

The code is divided into two folders. The `./backend` contains proxy server and `./frontend` contains react app.
  

### Steps To Run

Update the file `./envs/backend.env` file with COIN_MARKETCAP_API_KEY.


Backend:
- Run `docker-compose up backend`

Frontend:
- Run `docker-compose up frontend`

To Run Both
- Run `docker-compose up`
